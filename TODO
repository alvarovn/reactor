- Alternative log output to syslog

- Transitions that will never be executed again should be freed

- SELinux

- Check udev-ctrl.c -> ctrl_send()

- Change license of ./lib files to LGPL

- Unit and end-to-end tests

- Refactorize to remove unneeded abstraction

- Change sockets read() and write() to recv() and send()